### 자바스크립트 프레임워크에 대해서

> 참고:https://d2.naver.com/helloworld/7226235

이미 프레임워크의 장담점을 비교했었다. 하지만 장단점을 나열했을뿐 좀더 내자신과 관련되어 설명하는것이 면접에 도움이될것이라고 생각한다.



3대 프레임워크 리액트, 앵귤러, 뷰 가있다. 전부 자바스크립트와 웹을 기반을한다.

내가 SSAFY에서 배운것은 Vue !! 이유는 자바스크립트를 깊게 배울 시간이 없었기 떄문에 비교적 문법이 정형화있는 뷰가 커리큘럼이 있다고 생각한다. 물론 시장에서도 뷰라는 프레임워크가 생산성이 높다.





Vue.js

뷰는 자바스크립트 프로그레시브 프레임워크임 

라이브러리처럼 사용할수있게 설계되어서 라이브러리라고 할수있지만, 프로그레시브 프레임워크를 지향함

그럼 라이브러리와 프레임워크는 뭔차이임?

프레임워크는 코드를 프레임에 맞춰서 개발함  그래서 뷰는  js 파일이 아닌 `naem.vue`이런식으로 파일을 생성함!

프레임에 따라 코드를 개발하는데 최적화 되있고 더많은 기능을 제공함

코드 작성영역이 정해져있어서 가시성이 높고 생산성도 높음





React

리액트는 페이스북 광고팀이 실험적프로젝트로 시작한게 기원이다. 한국에서는 리액트에대한 비율이 상당히 높다. npm 다운로드수를 비교하면은 다른것보다 4.5배 차이가난다. 또한 하락이 아닌 성장하고 있다는 것은 중요한 지표!

특이한것은 프레임워크가 아니라 사용자 인터페이스 **라이브러리**임!!!!!

UI 컴포넌트를 만드일만하고 캡슐화를 잘시키기 때문에 재사용성이높다.



왜 이거쓰냐면 타입스크립트같은 자스의 정적표현이 리액트를 사용할때 편리하고 함수형 프로그래밍을 하려면  순수함수및 매개변수의 데이터타입을 고정하는게 필수인데, 이게 ts를 통해서 쉽게 처리가능



코드의 자유도 역시 높다. 그래서 그만큼 진입장벽이 높다. 연동되는 모듈도 앞도적으로 많아서 생테계적으로 풍성함. 하지만 그만큼 복잡함 이런 이유는 거의 UI라이브러리라서 생기는 문제





그럼 너는 왜 Vue.js를 썻나!

낮음 진입장벽이  동기이고 -> 추가적인 효과로는 가시성높아서 협업에 도움되었다.

이번에 왜 react js를 쓰냐?

한국의 프론트 업계가 리액트를 쓰는것이 다수이기 때문에 당연히 배워야할 것이라고 생각햇고 프로젝트로 실력을 비약적으로 높이고자함 -> 쓰다보니 자료가 방대해서 문제에 대한 해결이 빠른것이 좋았고 라이브러리에대해서 다양한 선택지가 있어서 좋았다, 아직 타입스크립트는 별루;  







MVC 프레임워크가 아님 -> MVC는 확장이어렵고 거대한 시스템에 어울리지않다고생각함

모델과 뷰의 수가 커질수록 양방향의 흐름이 점점 복잡해 질것이라고 생각함

![image-20210921210008255](C:\Users\PRO\AppData\Roaming\Typora\typora-user-images\image-20210921210008255.png)

그래서 Flux를 제안했는데 플럭스는 데이터 흐름이 단방향임!!!!

![image-20210921210000116](C:\Users\PRO\AppData\Roaming\Typora\typora-user-images\image-20210921210000116.png)

- Store: APP의 모든 데이터를 저장하며 MVC의 Model 역활입니다.
- Dispatcher: 모든 데이터를 관리하는 Controll 역활이며 Action 이 시작될 때 어떻게 Store가 업데이트되어야 하는지 결정합니다.
- View: Store가 변경된 경우같이 변경됩니다.
- 단방향: 데이터는 단방향으로 흐르고 데이터 계층이 자기가 영향을 미치는 View 업데이트 완료 후 다음 작업을 진행합니다.



### JSX: 템플릿을 사용하지 않는다.

자바스크립트는 강력한 언어라는 전제로 템플릿을 사용하지 않습니다.
템플릿을 사용하지 않아 뷰 로직과 뷰 문법을 통합하여 확장 및 유지 보수가 쉬워졌습니다.
템플릿이라고 오해할 수 있는 JSX를 원하면 사용할 수 있습니다.
JSX(JavaScript XML)는 페이스북에서 스펙을 정의한 XML과 비슷한 자바스크립트 확장 문법입니다.
자바스크립트에서 HTML과 자바스크립트 변수 및 속성 값을을 사용 할수 있게 해주며 *.jsx파일은 Webpack, Browserify로 자바스크립트로 컴파일 후 *.js 변환되어 사용하며 테스트 코드는 Bable로 별도의 컴파일 없이 사용할 수 있습니다. 그리고 이 툴 들이 ECMAScript6,7 지원을 하기 때문에 JSX를 사용시에 ECMAScript6,7도 같이 사용을 많이 합니다.



### Virtual DOM: 전체 DOM을 다시 그리지 않는다.

데이터가 변할 때 바뀐 부분만을 업데이트하여 "새로 고침" 하지 않아도 변경된 내용 확인이 가능합니다. 그래서 React를 적용하면 성능 향상을 볼 수 있으며 React의 큰 특징입니다.
이것은 가상 DOM(Virtual DOM)에 DOM Tree와 같은 전체 구조체를 가지고 있고 변경이 생기면 구조체를 비교(Diff)하여 최소한의 요소만 DOM에 적용합니다.



### Unidirectional Data Flow: 데이터가 양방향으로 흐르지 않는다.

양방향: 상위 컴포넌트에서 하위 컴포넌트로 혹은 하위 컴포넌트에서 상위 컴포넌트로
단방향: 상위 컴포넌트에서 하위 컴포넌트로

양방향 데이터 바인딩을 하게 되면 개발 비용은 일시적으로 적다고 느낄 수 있지만, 장기적으로 데이터 흐름을 이해하기 어려워 개발 비용이 증가합니다.
그래서 React는 위에서 아래로만 데이터가 흐르고, 아래에서 위로 올라갈 수는 없습니다.



### Server Side Rendering: 클라이언트에서만 실행되는 것은 아니다.

클라이언트에서만 렌더링을 하게 되면 HTML + Javascript + Data + View 순으로 진행하는 시간 소요로 초기 구동 속도가 느리다. React가 빠르다는 건 초기 구동 이후다.
또한, 자바스크립트를 실행하지 못하는 검색 엔진 봇들은 처음 렌더링 된 빈 HTML만 수집하기 때문에 콘텐츠가 없다고 판단하므로 검색 엔진 최적화(SEO)에 취약하다.
이런 단점을 해결하기 위해 서버 측에서 렌더링을 지원한다.